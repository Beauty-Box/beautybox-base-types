export const enum saleState {
    REPLACE = 'replace',
    COMPLETED = 'completed',
    PARTIAL_PAID = 'partialPaid',
    RETURNED = 'returned',
    UNPAID = 'unpaid',
    CANCEL = 'cancel',
}

export default saleState;
